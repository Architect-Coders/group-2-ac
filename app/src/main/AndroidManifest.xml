<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teamtwo.apilol">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
      
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".spells.SpellsLsitActivity"/>
        <activity android:name=".spells.SpellDetailActivity"/>
        <activity android:name=".matches.MatchListActivity"/>
        <activity android:name=".matches.MatchDetailActivity"/>
        <activity android:name=".items.ItemDetailActivity" />
        <activity android:name=".items.ItemListActivity" />
        <activity android:name=".champions.ChampionListActivity" />
        <activity android:name=".champions.ChampionDetailActivity" />
        <activity android:name=".summoners.SummonersDetailActivity" />
    </application>

</manifest>